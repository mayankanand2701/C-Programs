// Program to input the Character and Print its ASCII Value
#include <stdio.h>
int main(void)
{
    char c;
    int n;
    printf("Enter the Character = ");
    scanf("%c",&c);
    n=c;
    printf("The ASCII value of the given character as input is = %d",n);
}