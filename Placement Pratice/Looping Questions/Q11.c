#include <stdio.h>
int main(void)
{
    int num=0;
    int max=0,min=0,smax=0,smin=0;
    printf("Enter -1 to stop = ");
    do{
        printf("Enter the number = ");
        scanf("%d",&num);
        

    }while(num!=-1);
}